# Gradle
.gradle/
build/
!gradle/wrapper/gradle-wrapper.jar
!gradle-wrapper.properties

# Android / IDE
local.properties
*.iml
.idea/
.kotlin/
captures/
.cxx/
*.apk
*.aab
*.ap_
*.dex
*.class

# Keystores / secrets (never commit)
*.jks
*.keystore
keystore.properties
secrets.properties
!debug.keystore

# OS
.DS_Store
Thumbs.db

# Node (workflow scripts)
node_modules/

# Python (research prototyping venv — see research/requirements.txt)
research/.venv/
__pycache__/
*.pyc

# Test / coverage output
**/test-results/
**/reports/jacoco/
**/kover/

# TORGO / real-audio corpora — [measure-only], never committed (multi-GB, license-restricted).
# The eval harness reads a corpus path via -Dtorgo.dir; the corpus itself stays out of the repo.
**/torgo/**
*.tar.bz2
core/eval/build/torgo-report.md
*.onnx
*.onnx.data
