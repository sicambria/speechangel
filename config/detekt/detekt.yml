# Detekt config — builds upon the default rule set; formatting is owned by Spotless/ktlint.
build:
  maxIssues: 0
  excludeCorrectable: false

complexity:
  TooManyFunctions:
    thresholdInClasses: 20
    thresholdInInterfaces: 25
    thresholdInFiles: 25
  LongParameterList:
    functionThreshold: 8
    constructorThreshold: 9
  CyclomaticComplexMethod:
    threshold: 18
  LongMethod:
    threshold: 80
  NestedBlockDepth:
    threshold: 5

style:
  MagicNumber:
    active: false
  # Guard-clause early returns are idiomatic here; allow a few more than the default.
  ReturnCount:
    max: 6
  MaxLineLength:
    maxLineLength: 140
  UnusedPrivateMember:
    active: true

naming:
  FunctionNaming:
    # Compose @Composable functions are PascalCase by convention.
    ignoreAnnotated: ['Composable']

exceptions:
  TooGenericExceptionCaught:
    active: false
