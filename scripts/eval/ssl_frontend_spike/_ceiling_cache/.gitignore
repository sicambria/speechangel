# Large derived embedding caches + model checkpoints — regenerable via the *.py spikes; do NOT commit.
*.npz
*.pt
# Keep the small result JSONs (durable measurement evidence) — they are the banked numbers.
!*.json
