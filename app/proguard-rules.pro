# R8/ProGuard rules. R8 minification is currently disabled for release (see app/build.gradle.kts);
# these rules are kept ready for when device QA enables shrinking (docs/ROADMAP.md).
# Hilt, Room, and Compose ship their own consumer rules; add app-specific keeps here as needed.
